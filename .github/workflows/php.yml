name: PHP Composer

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: wishwell/backend
    steps:
    - uses: actions/checkout@v3
    - name: Set up PHP
      uses: shivammathur/setup-php@v2

    - name: Validate composer.json and composer.lock
      run: composer validate --strict

    - name: Cache Composer packages
      id: composer-cache
      uses: actions/cache@v3
      with:
        path: vendor
        key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-php-
    
    - name: Install dependencies
      working-directory: wishwell/backend
      run: composer install --prefer-dist --no-progress
    - name: Generate key
      working-directory: wishwell/backend/
      run: php artisan key:generate
      
    - name: Execute tests (Unit and Feature test) via PHPUnit
      working-directory: wishwell/backend
      run: vendor/bin/phpunit --coverage-html=coverage.html

      
    - name: Create coverage artifact
      uses: actions/upload-artifact@v2
      with:
          name: coverage-report
          path: wishwell/backend/coverage.html

    
    # - name: SonarQube Scan
    #   uses: sonarsource/sonarqube-scan-action@v2
    #   with:
    #       projectBaseDir:  wishwell/backend
    #       args: >
    #           -Dsonar.projectKey=ajayingale12345_wishwellFinal
    #           -Dsonar.projectName=wishwellFinal
    #           -Dsonar.projectVersion=2.0
    #           -Dsonar.php.coverage.reportPaths=coverage.xml  # Path to your PHPUnit coverage report
  
    #           env:
    #             SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    #             SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

